%h1
  Students

%table.table.table-hover
  %thead
    %tr
      %th ID
      %th Name
      %th Grade
  %tbody
    - students.each do |s|
      %tr
        %td= s.user.id
        %td= s.user.email
        %th
